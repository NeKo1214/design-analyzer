# Design Analyzer API Server 配置
# 复制为 .env 并填写实际值

# 服务端口（默认 3100）
PORT=3100

# 管理员密钥（用于调用 /api/admin/* 接口生成/吊销 DA Key）
# 未配置时仅允许 localhost 访问管理接口
ADMIN_SECRET=your-admin-secret-here

# CORS 允许来源（生产环境改为具体域名）
CORS_ORIGIN=*